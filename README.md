# podlet2nix

A CLI utility that translates podlet output to qudlet-nix options

## Usage

This utility is meant to be used in a pipeline with pdolet.

```bash
$ podlet compose docker-compose.yml | podlet2nix
```
